function longestConsec(strarr, k) {
    let array = [];
    let numbers = '';
    for (const a of strarr) {
        numbers = a.split('');
        array.push(numbers.length);
    }
    
    while{}

    return array;
}

console.log(longestConsec(['zone', 'abigail', 'theta', 'form', 'libe', 'zas'], 2));
